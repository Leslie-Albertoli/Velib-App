package com.example.velib_app.bdd

/*import android.content.Context
import androidx.room.Database
import androidx.room.Room
import androidx.room.RoomDatabase

@Database(entities = [StationDetailsEntity::class], version = 2)
abstract class StationDetailsDatabase : RoomDatabase() {
    abstract fun stationDetailsDao(): StationDetailsDao


    companion object {
        fun createDatabase(ctx: Context): StationDetailsDatabase {
            return Room.databaseBuilder(
                ctx,
                StationDetailsDatabase::class.java, "listStationDetails.db"
            )
                .fallbackToDestructiveMigration()
                .build()
        }
    }
}*/